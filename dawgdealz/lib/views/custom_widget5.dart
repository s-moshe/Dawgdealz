import 'package:flutter/material.dart';

class CustomWidget5 extends StatelessWidget {
  const CustomWidget5({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This is for the account page.');
  }
}
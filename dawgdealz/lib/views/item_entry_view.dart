import 'package:flutter/material.dart';

class ItemEntryView extends StatelessWidget {
  const ItemEntryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This is for the discovery page.');
  }
}
import 'package:flutter/material.dart';

class CustomWidget4 extends StatelessWidget {
  const CustomWidget4({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This is for the inbox page.');
  }
}